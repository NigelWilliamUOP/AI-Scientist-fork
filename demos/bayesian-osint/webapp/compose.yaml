services:
  observatory:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    environment:
      APP_DATA_DIR: /data
    volumes:
      - osint-data:/data
    restart: unless-stopped
volumes:
  osint-data:
