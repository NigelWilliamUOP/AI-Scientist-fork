function render(): void { if (!workspace || !user)
    return; const w = workspace; const admitted = w.sources.filter(s => !s.excluded); const originCount = new Set(admitted.map(s => s.origin_group)).size; const captures = admitted.filter(s => s.representation === 'original_http_capture').length; const historical = w.session.mode === 'HISTORICAL_RECONSTRUCTION'; const frozen = w.session.mode === 'RECORDED_REPLAY'; root.innerHTML = `<div class="app-shell"><aside class="sidebar" aria-label="Source explorer"><div class="brand"><span class="brand-mark">B</span>OSINT<span class="brand-accent">BAYES</span></div><div class="sidebar-subtitle">EVIDENCE OBSERVATORY</div><div class="project-marker"><span class="project-icon">SC</span><div><strong>Sea Change</strong><small>Portsmouth shore power</small></div></div><div class="catalogue-head"><h2>Source explorer</h2><span>${admitted.length}/${w.sources.length}</span></div><label class="sr-only" for="source-filter">Filter sources</label><input id="source-filter" type="search" placeholder="Filter sources…" value="${esc(filter)}"><div class="source-list" id="source-list">${sourceCards()}</div><div class="sidebar-foot"><span class="tiny-dot"></span>Public information only<br><small>Origins are not assumed independent.</small></div></aside><main id="main" class="workspace"><header class="topbar"><div class="breadcrumb">OSINT Bayes <span>/</span> HEIF demonstrator</div><div class="connection-status"><button data-action="settings"><span class="tiny-dot amber"></span>${user.ai_status === 'CONFIGURED_NOT_LIVE_VERIFIED' ? 'AI configured' : 'AI not connected'}</button><button data-action="settings"><span class="tiny-dot amber"></span>${user.drive_status === 'DRIVE_NOT_CONNECTED' ? 'Drive not connected' : 'Drive configured'}</button>${action('logout', 'Sign out', '', 'quiet')}</div></header><section class="page-heading"><div><div class="eyebrow">PUBLIC EVIDENCE INVESTIGATION</div><h1>What does the evidence establish?</h1><p>Portsmouth’s shore-power operating milestone</p></div><div class="toolbar">${action('tour', 'Guided demo', '', 'quiet')}${action('refresh', icon('refresh') + ' Refresh originals', busy || historical || frozen || user.role !== 'owner' ? 'disabled' : '', '')}${action('export', icon('export') + ' Export audit', busy ? 'disabled' : '', 'primary')}</div></section><section class="mode-strip ${historical ? 'historical' : ''}"><div><span class="tiny-dot ${historical ? 'amber' : ''}"></span><strong>${historical ? 'HISTORICAL RECONSTRUCTION' : frozen ? 'FROZEN EVIDENCE REPLAY' : 'CURRENT INVESTIGATION'}</strong><span>${historical ? 'Publisher cutoff: ' + esc(date(w.session.cutoff)) + ' · inspected later, not captured then' : frozen ? 'Retained evidence only. New network and model calls disabled.' : `${captures} original HTTP captures / ${admitted.length} admitted records · curated extracts are not live captures`}</span></div>${action('mode', 'Change mode', '', 'quiet')}</section><section class="metrics" aria-label="Evidence status"><article><span>Admitted evidence</span><strong>${String(admitted.length).padStart(2, '0')}</strong><small>${w.sources.length - admitted.length} excluded in this scenario</small></article><article><span>Declared origins</span><strong>${String(originCount).padStart(2, '0')}</strong><small>Repeated reports stay grouped</small></article><article><span>Accepted assessment</span><strong class="text-metric">${w.assessment.stale ? 'Review required' : w.assessment.body.status === 'HUMAN_REVIEWED' ? 'Human reviewed' : 'Not yet reviewed'}</strong><small>Revision ${w.assessment.revision} · approval is explicit</small></article><article><span>AI spend committed</span><strong class="text-metric">$${w.budget_committed_usd.toFixed(4)}</strong><small>Includes unresolved reservations</small></article></section>${tour >= 0 ? tourView() : ''}<div class="investigation-layout"><section class="canvas-panel"><nav class="view-tabs" aria-label="Investigation views">${['network', 'map', 'timeline', 'brief', 'training'].map(t => action('tab', icon(t === 'training' ? 'settings' : t) + (t === 'training' ? 'Sensitivity lab' : t[0].toUpperCase() + t.slice(1)), `data-tab="${t}" aria-pressed="${tab === t}"`, tab === t ? 'active' : '')).join('')}<span class="view-label">${tab === 'training' ? 'SYNTHETIC ONLY' : 'REAL EVIDENCE'}</span></nav><div id="canvas">${canvas()}</div></section><aside class="evidence-panel" aria-label="Evidence details">${detailView()}</aside></div><section class="question-panel"><div class="question-title"><div><div class="eyebrow">EVIDENCE-BOUNDED RESEARCH</div><h2>Ask, inspect, challenge.</h2></div>${statusTag(user.ai_status === 'CONFIGURED_NOT_LIVE_VERIFIED' ? 'Endpoint preflight required' : 'No model configured', 'warn')}</div><label for="research-question" class="sr-only">Research question</label><textarea id="research-question" maxlength="4000" rows="2">${esc(query)}</textarea><div class="question-bottom"><div class="presets">${action('preset', 'What did the correction change?', 'data-q="What exactly did the correction change?"', 'chip')}${action('preset', 'Does £0 mean free?', 'data-q="Does the structured zero contract-value field establish that the equipment was free?"', 'chip')}</div><div class="question-actions"><label class="small"><input id="challenge" type="checkbox" ${user.ai_status !== 'CONFIGURED_NOT_LIVE_VERIFIED' ? 'disabled' : ''}> Second-pass challenge</label>${action('compile', 'Compile evidence brief', busy || frozen ? 'disabled' : '', '')}${action('ask', 'Ask AI ' + icon('arrow'), busy || frozen || user.ai_status !== 'CONFIGURED_NOT_LIVE_VERIFIED' ? 'disabled title="Configure OpenRouter on the server first"' : '', 'primary')}</div></div><p class="small muted">The template brief summarises admitted records without AI. Model-generated drafts require quotation checks and human review.</p></section>${draft ? draftView() : ''}<section class="activity"><div class="activity-head"><h2>${busy ? '<span class="tiny-dot amber"></span> ' : ''}Activity ledger</h2><span>Recorded backend events, not animated agents</span></div><div class="activity-events">${w.events.slice(0, 7).map(e => `<div><time>${esc(new Date(e.at).toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit', second: '2-digit', timeZone: 'UTC' }))} UTC</time><strong>${esc(label(e.kind))}</strong><span>${eventDetail(e)}</span></div>`).join('')}</div></section><footer class="workspace-footer"><span>Release ${esc(user.release)} · authenticated ${esc(user.role)} workspace</span><span>Not a calibrated forecast. Not an operational port system.</span></footer></main></div>`; wire(); if (tab === 'map' && mapEnabled)
    loadTiles(); }
function eventDetail(e: LedgerEvent): string { try {
    const d = JSON.parse(e.detail);
    return esc(d.source_id || d.error || d.mode || d.export_id || d.kind || '');
}
catch {
    return '';
} }
